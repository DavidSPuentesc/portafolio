import {describe,expect,it} from 'vitest';import {validateContact} from '../../src/lib/contact';import {createRateLimiter} from '../../src/lib/rate-limit';
describe('contact safety',()=>{it.each([[{email:'bad',message:'Hola'},'invalid'],[{email:'a@b.co',message:'x'.repeat(6001),intent:'hiring',locale:'es',name:'A'},'too_large'],[{email:'a@b.co',message:'Necesito ayuda con este proyecto',website:'bot.example',intent:'business',locale:'es',name:'A'},'spam']])('rejects unsafe input',(input,code)=>expect(validateContact(input)).toEqual(expect.objectContaining({ok:false,code})));it('blocks the sixth request inside ten minutes',async()=>{const counts=new Map<string,number>();const fake={incr:async(k:string)=>{const n=(counts.get(k)??0)+1;counts.set(k,n);return n},expire:async()=>1};const limiter=createRateLimiter(fake,{limit:5,windowSeconds:600});for(let i=0;i<5;i++)expect(await limiter.check('203.0.113.8')).toBe(true);expect(await limiter.check('203.0.113.8')).toBe(false);});});
